description "upstart"

start on (filesystem)
stop on runlevel [016]

respawn
setuid root
setgid nogroup
chdir {{ repository_path }}

exec gunicorn application:application --bind=unix:/tmp/tcat.sock --workers 3 --user=root --log-level=debug 
